#include "monty.h"
/**
 * comment -  doesn’t do anything (let the user write comments).
 * @stack: linked list of the data as stack
 * @line_number: line number
 * Return: void
 */
void comment(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
